let
	local
		var a := 5;
		var b := 15
	in
		!var c := a*b*2 ! faltan declaraciones en la parte pública
	end;
	var d := 5
in
	putint(c)
end

! Error: Falta declaración en private que usa la privada